{
  "name": "desafio7",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "parcel": "^2.8.3"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /desafio-5",
    "deploy": "gh-pages -d build",
    "builds-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
